import styled from "styled-components";

const StyledRightSideBar = styled.nav`
  height: 100%;
  margin-top: 160px;
  right: 0;
  z-index: 1000;
  position: fixed;
`;

export default StyledRightSideBar;
